\begin{figure}[h]
\centering
\begin{tcolorbox}[
    enhanced,
    colback=blue!5!white,
    colframe=blue!75!black,
    arc=5mm,
    boxrule=1.5pt,
    title=Oracle Service Architecture,
    fonttitle=\bfseries,
    coltitle=white,
    attach boxed title to top left={yshift=-2mm, xshift=5mm},
    boxed title style={colback=blue!75!black, rounded corners},
    shadow={2mm}{-2mm}{0mm}{black!50},
    drop fuzzy shadow
]
\begin{tikzpicture}[node distance=1.5cm, auto]
    % Define styles
    \tikzstyle{block} = [rectangle, draw, fill=blue!20, text width=8em, text centered, rounded corners, minimum height=3em]
    \tikzstyle{line} = [draw, -latex']
    \tikzstyle{cloud} = [draw, ellipse, fill=red!20, node distance=3cm, minimum height=2em]
    
    % Place nodes
    \node [block] (sources) {Data Sources};
    \node [block, right=of sources] (providers) {Data Providers};
    \node [block, right=of providers] (oracle) {Oracle Service};
    \node [block, below=of oracle] (blockchain) {Neo Blockchain};
    \node [block, left=of blockchain] (apps) {Applications};
    
    % Draw edges
    \path [line] (sources) -- node {Raw Data} (providers);
    \path [line] (providers) -- node {Verified Data} (oracle);
    \path [line] (oracle) -- node {Price Feeds} (blockchain);
    \path [line] (blockchain) -- node [above] {Data Access} (apps);
    \path [line] (oracle) -- node [left] {Direct API} (apps);
\end{tikzpicture}
\end{tcolorbox}
\caption{Oracle Service Architecture and Data Flow}
\label{fig:oracle-architecture}
\end{figure}
