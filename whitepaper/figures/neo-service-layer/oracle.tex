% This file should only be \input{} in a document that loads the tikz package
% Do not compile this file directly with pdflatex

\begin{figure}[ht]
\centering
\begin{tikzpicture}[font=\scriptsize, node distance=5.5cm]
    % Define nodes using our consistent style
    \node[block] (sources) {Data Sources};
    \node[block, right of=sources, node distance=5.5cm] (providers) {Data Providers};
    \node[block, right of=providers, node distance=5.5cm] (oracle) {Oracle Service};
    \node[block, below of=oracle, node distance=3.5cm] (blockchain) {Neo Blockchain};
    \node[block, left of=blockchain, node distance=5.5cm] (apps) {Applications};
    
    % Connections with our consistent style
    \draw[connector] (sources) -- node[above-arrow-text] {Raw Data} (providers);
    \draw[connector] (providers) -- node[above-arrow-text] {Verified Data} (oracle);
    \draw[connector] (oracle) -- node[above-arrow-text, right] {Price Feeds} (blockchain);
    \draw[connector] (blockchain) -- node[above-arrow-text] {Data Access} (apps);
    \draw[connector] (oracle) to[bend right=30] node[above-arrow-text, left] {Direct API} (apps);
\end{tikzpicture}
\caption{Oracle Service Architecture and Data Flow}
\label{fig:oracle-architecture}
\end{figure}
