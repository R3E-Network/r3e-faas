\begin{figure}[h]
\centering
\begin{tcolorbox}[
    enhanced,
    colback=blue!5!white,
    colframe=blue!75!black,
    arc=5mm,
    boxrule=1.5pt,
    title=Neo Ecosystem Integration,
    fonttitle=\bfseries,
    coltitle=white,
    attach boxed title to top left={yshift=-2mm, xshift=5mm},
    boxed title style={colback=blue!75!black, rounded corners},
    shadow={2mm}{-2mm}{0mm}{black!50},
    drop fuzzy shadow
]
\begin{tikzpicture}[node distance=1.5cm, auto]
    % Define styles
    \tikzstyle{block} = [rectangle, draw, fill=blue!20, text width=8em, text centered, rounded corners, minimum height=3em]
    \tikzstyle{line} = [draw, -latex']
    \tikzstyle{cloud} = [draw, ellipse, fill=red!20, node distance=3cm, minimum height=2em]
    
    % Place nodes
    \node [block] (neo) {Neo N3 Blockchain};
    \node [block, right=of neo] (contract) {NeoContract};
    \node [block, right=of contract] (service) {Neo Service Layer};
    \node [block, below=of service] (identity) {Digital Identity};
    \node [block, below=of contract] (oracle) {Oracle Service};
    
    % Draw edges
    \path [line] (neo) -- node {Core Platform} (contract);
    \path [line] (contract) -- node {Integration} (service);
    \path [line] (service) -- node {Authentication} (identity);
    \path [line] (service) -- node [left] {Data Feeds} (oracle);
    \path [line] (oracle) -- node [above] {External Data} (contract);
    \path [line] (identity) -- node [above] {Verification} (neo);
\end{tikzpicture}
\end{tcolorbox}
\caption{Neo Ecosystem Integration Architecture}
\label{fig:neo-integration}
\end{figure}
