/// <reference types="node" />
import { EventEmitter } from 'events';
import { AxiosRequestConfig } from 'axios';
import { JsonRpcConnection, JsonRpcPayload } from './types';
export declare class HttpConnection implements JsonRpcConnection {
    url: string;
    private axiosConfig?;
    events: EventEmitter;
    private api;
    private registering;
    constructor(url: string, axiosConfig?: AxiosRequestConfig | undefined);
    get connected(): boolean;
    get connecting(): boolean;
    on(event: string, listener: any): void;
    removeListener(event: string, listener: any): void;
    open(url?: string): Promise<void>;
    close(): Promise<void>;
    send(payload: JsonRpcPayload, _context?: any): Promise<void>;
    private register;
    private onOpen;
    private onClose;
    private onPayload;
    private onError;
}
