import { ErrorResponse } from './types';
export declare enum StandardErrorCodes {
    ParseError = -32700,
    InvalidRequest = -32600,
    MethodNotFound = -32601,
    InvalidParams = -32602,
    InternalError = -32603,
    NetworkError = -32000
}
export declare function isValidErrorCode(code: unknown): code is number;
export declare function isStandardErrorCode(code: number): code is StandardErrorCodes;
export declare function getStandardErrorResponse(code: number): ErrorResponse;
export declare class RpcError extends Error implements ErrorResponse {
    code: number;
    data?: any;
    constructor(error: ErrorResponse);
}
